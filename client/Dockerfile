FROM node:20-slim

WORKDIR client
COPY . .
RUN yarn

ARG REACT_APP_STRAPI_AUTH_TOKEN
ENV REACT_APP_STRAPI_AUTH_TOKEN $REACT_APP_STRAPI_AUTH_TOKEN

ARG REACT_APP_STRAPI_URL
ENV REACT_APP_STRAPI_URL $REACT_APP_STRAPI_URL

EXPOSE 5173
CMD [ "yarn","dev" ]